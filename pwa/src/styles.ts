import { CSSProperties } from "react";

export const styles = {
  container: {
    display: "flex",
    flexDirection: "column",
    flexWrap: "wrap",
    alignContent: "center",
    margin: 8,
    overflow: "auto",
    padding: 8,
  } as CSSProperties,
};
